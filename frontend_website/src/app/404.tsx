import React from "react";

const unknownEndpoint = () => {
  return (
    <html>
      <head />

      <body>
        <div>unknownEndpoint</div>
      </body>
    </html>
  );
};

export default unknownEndpoint;
